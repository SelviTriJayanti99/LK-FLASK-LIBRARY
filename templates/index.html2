<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data K-pop</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            margin: 0;
            padding: 0;
        }
        h1 {
            text-align: center;
            margin-top: 20px;
        }
        table {
            width: 80%;
            margin: 20px auto;
            border-collapse: collapse;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        table, table th, table td {
            border: 1px solid #ddd;
        }
        table th, table td {
            padding: 12px;
            text-align: center;
        }
        table th {
            background-color: #f2f2f2;
        }
        .stats {
            width: 80%;
            margin: 20px auto;
            text-align: center;
        }
    </style>
</head>
<body>

<h1>Daftar Grup K-pop</h1>

<table>
    <thead>
        <tr>
            <th>Nama Grup</th>
            <th>Genre Musik</th>
            <th>Tahun Debut</th>
            <th>Agensi</th>
        </tr>
    </thead>
    <tbody>
        {% for group in kpop_groups %}
        <tr>
            <td>{{ group['Nama Grup'] }}</td>
            <td>{{ group['Genre Musik'] }}</td>
            <td>{{ group['Tahun Debut'] }}</td>
            <td>{{ group['Agensi'] }}</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

<div class="stats">
    <h2>Rata-rata Tahun Debut: {{ average_debut }}</h2>
    <h2>Median Tahun Debut: {{ median_debut }}</h2>
    <h2>Standar Deviasi Tahun Debut: {{ std_debut }}</h2>
    <h2>Cumulative Sum Tahun Debut: {{ cumulative_sum }}</h2>
</div>

</body>
</html>
